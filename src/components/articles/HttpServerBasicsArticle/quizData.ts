import type { QuizQuestion } from '@/components/ui/QuizBlock/QuizBlock';

export const QUIZ_QUESTIONS: QuizQuestion[] = [
  {
    id: 'hsb1',
    difficulty: 'easy',
    question: 'Что делает server.listen(3000)?',
    options: [
      'Отправляет запрос на порт 3000',
      'Говорит серверу начать принимать входящие соединения на порту 3000',
      'Открывает браузер по адресу localhost:3000',
      'Создаёт новый HTTP-сервер',
    ],
    correct: 1,
    explanation:
      'listen() — команда «начни слушать». Сервер занимает порт 3000 и ждёт входящих подключений. Только после этого к нему можно обратиться из браузера или другой программы. createServer() создаёт сервер, listen() запускает его.',
  },
  {
    id: 'hsb2',
    difficulty: 'easy',
    code: `const server = createServer((req, res) => {
  res.writeHead(404);
  res.end('Not found');
});`,
    question: 'Что означает число 404 в res.writeHead(404)?',
    options: [
      'Порт, на который отправляется ответ',
      'Размер ответа в байтах',
      'Код статуса HTTP — стандартный способ сказать «страница не найдена»',
      'Время ожидания ответа в миллисекундах',
    ],
    correct: 2,
    explanation:
      'HTTP-статус коды — трёхзначные числа, которые сервер возвращает вместе с ответом. 200 — всё хорошо, 404 — не найдено, 500 — ошибка сервера. Браузер и другие программы смотрят на этот код чтобы понять, успешен ли запрос.',
  },
  {
    id: 'hsb3',
    difficulty: 'medium',
    code: `createServer((req, res) => {
  if (req.url === '/users' && req.method === 'GET') {
    res.writeHead(200, { 'Content-Type': 'application/json' });
    res.end(JSON.stringify([{ id: 1, name: 'Alice' }]));
  }
});`,
    question: 'Зачем указывать Content-Type: application/json?',
    options: [
      'Без него сервер не сможет вызвать JSON.stringify',
      'Это сообщает получателю в каком формате данные — чтобы он знал как их разобрать',
      'Это обязательно для всех ответов, иначе браузер не примет ответ',
      'Это ограничивает доступ к данным только для JSON-клиентов',
    ],
    correct: 1,
    explanation:
      'Content-Type — заголовок, который говорит получателю "в каком формате данные". application/json означает JSON. Без него браузер или другая программа получат текст и не будут знать, что его надо разобрать как JSON. Это как подписать конверт — данные те же, но получатель знает что внутри.',
  },
  {
    id: 'hsb4',
    difficulty: 'medium',
    code: `// Как прочитать тело POST-запроса?
createServer((req, res) => {
  let body = '';
  req.on('data', chunk => { body += chunk; });
  req.on('end', () => {
    const data = JSON.parse(body);
    // теперь data — объект
  });
});`,
    question: 'Почему тело запроса читается через события, а не сразу целиком?',
    options: [
      'Это особенность Node.js — он не умеет читать данные иначе',
      'Данные могут приходить кусками по сети, событийная модель позволяет обрабатывать их по мере поступления',
      'Из соображений безопасности — так сложнее перехватить данные',
      'Это старый способ, современный Node.js читает тело через await req.json()',
    ],
    correct: 1,
    explanation:
      'Сетевые данные приходят кусками (chunks). Node.js получает их по мере поступления и генерирует событие data для каждого куска. Когда всё пришло — событие end. Это та же неблокирующая модель что и в основе Node.js: не ждать пока придёт всё, обрабатывать по мере готовности.',
  },
  {
    id: 'hsb5',
    difficulty: 'hard',
    code: `// Сервер с 20 маршрутами выглядит так:
if (url === '/' && method === 'GET') { ... }
else if (url === '/users' && method === 'GET') { ... }
else if (url === '/users' && method === 'POST') { ... }
else if (url === '/users/1' && method === 'GET') { ... }
else if (url === '/users/1' && method === 'PUT') { ... }
// ... ещё 15 строк`,
    question: 'Какую главную проблему решают библиотеки вроде Express?',
    options: [
      'Делают сервер быстрее — встроенный http медленный',
      'Добавляют поддержку HTTPS — без них шифрование невозможно',
      'Убирают повторяющийся код: роутинг, парсинг тела, заголовки — чтобы не писать if-else на 200 строк',
      'Позволяют использовать TypeScript — без них типизация невозможна',
    ],
    correct: 2,
    explanation:
      'Встроенный http модуль — низкоуровневый инструмент. С ростом числа маршрутов код превращается в огромную цепочку if-else. Библиотеки вроде Express дают структуру: app.get("/users", handler), app.post("/users", handler) — каждый маршрут в одну строку. Плюс автоматически разбирают тело запроса, query-параметры, заголовки.',
  },
];
